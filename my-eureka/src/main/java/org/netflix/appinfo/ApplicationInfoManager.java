package org.netflix.appinfo;

/**
 * 初始化注册所需信息的类
 * <tt> Eureka Server </ tt>并由其他组件发现。
 * <p>
 * 注册所需的信息由用户通过
 *{EurekaInstanceConfig}中合同定义的配置}
 * AWS客户端可以使用或扩展{ CloudInstanceConfig}。
 * 其他非AWS客户端也可以使用或扩展
 */
public class ApplicationInfoManager {
}
